# ai-girlfriend-chat.complex
The Canvas is a frontend-only chat interface using HTML, Tailwind CSS, and JavaScript. It allows selection from four AI girlfriends and simulates conversations. Currently, it's a mockup—the AI's replies are hardcoded within getAIResponse using simple keyword checks (like 'hello' or 'love you'). 
